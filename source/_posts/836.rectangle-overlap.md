---
title: 836.矩形重叠
categories: 简单
index_img: /img/maple-tree-8010467_1920.jpg
banner_img: /img/maple-tree-8010467_1920.jpg
date: 2024-12-03 23:01:00
tags:
  - 简单难度
  - 数学
---

## [矩形重叠](https://leetcode.cn/problems/rectangle-overlap/)

矩形以列表 [x1, y1, x2, y2] 的形式表示，其中 (x1, y1) 为左下角的坐标，(x2, y2) 是右上角的坐标。矩形的上下边平行于 x 轴，左右边平行于 y 轴。

如果相交的面积为正，则称两矩形重叠。需要明确的是，只在角或边接触的两个矩形不构成重叠。

给出两个矩形 rec1 和 rec2 。如果它们重叠，返回 true ；否则，返回 false 。

<!-- more -->

示例 1：

> 输入：rec1 = [0,0,2,2], rec2 = [1,1,3,3]
> 输出：true

示例 2：

> 输入：rec1 = [0,0,1,1], rec2 = [1,0,2,1]
> 输出：false

示例 3：

> 输入：rec1 = [0,0,1,1], rec2 = [2,2,3,3]
> 输出：false

## 解析

两个矩形重叠等价于在 x 轴和 y 轴上的投影都有重叠。反向思考更简单——判断不重叠的情况取反。

```javascript
var isRectangleOverlap = function (rec1, rec2) {
  return rec1[0] < rec2[2] && rec2[0] < rec1[2] &&
         rec1[1] < rec2[3] && rec2[1] < rec1[3];
};
```

时间复杂度 O(1)，空间复杂度 O(1)。
