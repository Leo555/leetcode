---
title: 692.前K个高频单词
categories: 中等
index_img: /img/cliff-gc08bc35e1_1920.jpg
banner_img: /img/cliff-gc08bc35e1_1920.jpg
date: 2024-11-12 21:58:00
tags:
  - 中等难度
  - 哈希表
  - 排序
---

## [前K个高频单词](https://leetcode.cn/problems/top-k-frequent-words/)

给定一个单词列表 words 和一个整数 k，返回前 k 个出现次数最多的单词。返回的答案应该按单词出现频率由高到低排序。如果不同的单词有相同出现频率，按字典序排序。

<!-- more -->

## 解析

```javascript
var topKFrequent = function (words, k) {
  const map = new Map();
  for (const w of words) map.set(w, (map.get(w) || 0) + 1);
  return [...map.entries()]
    .sort((a, b) => b[1] - a[1] || a[0].localeCompare(b[0]))
    .slice(0, k)
    .map(e => e[0]);
};
```
